1. Improve caching mechanism: 
    - Data fetching should check config swithces - USE_CACHED == True or False?
    - It should work more like sector fetching checks cache.
        stock list (Currently S&P 500 scraped from wikipedia),prices, earnings, etc:
        fetching errors, 
        downtime, 
        alternate sources in case main one fails,
        datatype checking

2. REACTION_THRESHOLD - probably shouldn't be a static number (Check GPT)

3. For reports:
    base_xtreme_prob: the unconditional historical probability of is_extreme_reaction == 1 across all earnings events in your reference universe. Based on these outputs, that is currently about 4.36%.

    hist_xtreme_prob: the historical extreme probability associated with the stock’s current score bucket or decile. For page 1, this should be the decile-conditioned probability, not the stock’s own raw historical frequency.

    risk_lift: hist_xtreme_prob / base_xtreme_prob.

4. Timing Danger - uses proximity_risk but on earnings_days its always 100 which could be useless. Check GPT for solutions.

5. Move these OUT of df, create a separate table earnings_outcomes with:
    """ Used for: evaluation, PDFs, credibility (“historically this stock moves X%”) """
    reaction_3d,1d,5d
    sector_mean_d
    relative_to_sector
    reportedEPS
    estimatedEPS
    surprise
    surprise_bucket

6.  Possible useful features to implement in the feature (Stage 2):
    Structural risk features, these 3 explain why a move might be violent:
    avg_dollar_volume (or proxy)
    bid_ask_proxy (even a crude one)
    gap_frequency

    market_cap_bucket
    liquidity_proxy (avg dollar volume)
    gap_frequency (how often it gaps > X%)

    From old project:
    past_up_freq
    past_down_freq
    past_nochange_freq
    past_consistency_3d
    past_consistency_10d
    prev_earnings_direction

    relative_to_sector = drift_10d − sector_drift_10d
    sector_volatility_score =
        sector_vol_10d / rolling_median(sector_vol_10d, 120)

    reaction_volatility =    # This is basically: “Does this stock usually move on earnings?”
        (past_up_freq + past_down_freq) > past_nochange_freq

7.If you want true quarter-specific predictive signal,
You now need features that vary quarter-to-quarter:

dispersion in analyst revisions
option-implied skew shifts pre-earnings
short interest change
vol_ratio_10_to_30 spike
sector co-movement breakdown
recent surprise volatility acceleration

Right now your engine is mostly static.


Output:

A good Breakwater report (worth $300–1,000+):
    Answers a decision
    “Do not hold through earnings”
    “Only tradable with options”
    “Size ≤ 0.5%”
    “High portfolio correlation risk this week”

    Is cross-sectional
    ranks risks
    highlights clusters
    shows relative danger

    Is opinionated
    not neutral
    not hedgey
    not “could go up or down”

    Is repeatable
    same format
    same semantics
    same sections every time