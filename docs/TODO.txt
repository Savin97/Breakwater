1. Data fetching should check config swithces - USE_CACHED == True or False?

2. Improve caching mechanism  - should work more like sector fetching checks cache.
    stock list (Currently S&P 500 scraped from wikipedia),prices, earnings, etc:
        fetching errors, 
        downtime, 
        alternate sources in case main one fails,
        datatype checking
    Also storage of this data - *parquet* instead of csv.

3. REACTION_THRESHOLD - probably shouldn't be a static number (Check GPT)

4. What you should focus on this week:
Make pipeline run cleanly end-to-end (dirs, caching, fail-fast checks)
Implement real Stage 4 backtest + credibility tables
Add explanations (reasons) in Stage 3
Generate the first earnings-week PDF/HTML report


- Move these OUT of df, create a separate table earnings_outcomes with:
    """ Used for: evaluation, PDFs, credibility (“historically this stock moves X%”) """
    reaction_3d,1d,5d
    sector_mean_d
    relative_to_sector
    reportedEPS
    estimatedEPS
    surprise
    surprise_bucket

- Features to implement in the feature (Stage 2):
    Structural risk features, these 3 explain why a move might be violent:
    avg_dollar_volume (or proxy)
    bid_ask_proxy (even a crude one)
    gap_frequency

    market_cap_bucket
    liquidity_proxy (avg dollar volume)
    gap_frequency (how often it gaps > X%)


Output:

A good Breakwater report (worth $300–1,000+):
    Answers a decision
    “Do not hold through earnings”
    “Only tradable with options”
    “Size ≤ 0.5%”
    “High portfolio correlation risk this week”

    Is cross-sectional
    ranks risks
    highlights clusters
    shows relative danger

    Is opinionated
    not neutral
    not hedgey
    not “could go up or down”

    Is repeatable
    same format
    same semantics
    same sections every time